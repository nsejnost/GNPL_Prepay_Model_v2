pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
openpyxl>=3.1
pyarrow>=14.0
requests>=2.31

# Optional: only needed for the live GNMA download path in main.py
# (skipped automatically when running with --skip-download). Uncomment
# if you want to authenticate to GNMA Disclosure and pull mfplmon3
# files directly from Replit.
# playwright>=1.40
